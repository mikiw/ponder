{
  "name": "ponder-demo",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "ponder dev",
    "start": "ponder start --schema public",
    "serve": "ponder serve --schema public",
    "codegen": "ponder codegen"
  },
  "dependencies": {
    "ponder": "workspace:*",
    "viem": "2.30.1"
  },
  "devDependencies": {
    "typescript": "5.0.4"
  }
}
